# Copy this file to the same folder and name it exactly:  .env
# (dotenv does not read .env.example)

PORT=5000
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES=7d
# Must match the email in the users table (lowercase). On each server start, this user gets role admin.
ADMIN_EMAIL=admin@gmail.com
