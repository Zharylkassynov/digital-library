# Optional: set when the frontend is served from a different origin than the backend.
# Leave unset in development (Vite proxy forwards /api and /images to http://localhost:5000).
# VITE_API_URL=http://localhost:5000
